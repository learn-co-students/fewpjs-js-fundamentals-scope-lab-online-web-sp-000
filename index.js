let animal = "";

function myAnimal() {
  ////need to be dog
  let animal = "dog";
  return animal
}

function yourAnimal() {
  // How can we make sure that this function
  // and the above function both pass?
  // P.S.: You can't just hard-code 'cat' below
  let animal = "cat";

  return animal //need to be cat
}

function add2(n) {
  const   two = 2;
  return n + two;

  // Feel free to move things around!
}